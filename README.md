```
# 태그만들기
git tag v0.0.2
git push origin v0.0.2

# 디펜던시추가하기
implementation("com.github.quick-swap-backend:commons:v0.0.2")
```


```
SecurityFilterChain, AbstractJwtTokenResolver 의 구현체만 구현하면
AuthenticationContext 를 통해 유저의 인증정보를 사용 할 수 있습니다.
```